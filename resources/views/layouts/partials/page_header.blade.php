<section class="page-header">
    <div class="container">
        <h1 class="page-title">{{ $title }}</h1>
        @if(isset($sub)) <h2 class="page-subtitle">{{ $sub }}</h2> @endif
    </div>
</section>